meta {
  name: Change User Role
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/users/{{userId}}/role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "roleId": "{{roleId}}"
  }
}

